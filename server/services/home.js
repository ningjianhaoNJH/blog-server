// const homeModel = require('../models/home');
const homeService = {
    async getHomeService(opt) {
        // 获取首页用户信息
        // 获取最新文章
        // 获取首页用户分类
        // 获取首页用户热门文章
        // 获取首页用户归档
        // 获取今日标签
        let result = null;
        return result
    }
};
module.exports = homeService;
